using UnityEngine;
using System.Collections;

public class Pickupable : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("this can be picked up!");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
